    container.innerHTML = `
      <table class="data-table" style="width:100%; border-collapse:collapse; table-layout:auto;">
        <thead>
          <tr style="border-bottom:1px solid var(--chrome-border); text-align:left; color:var(--text-muted); font-size:0.8rem;">
            <th style="padding:10px 8px;">ID MEMBER</th>
            <th style="padding:10px 8px;">NAMA & KONTAK MEMBER</th>
            <th style="padding:10px 8px;">KLUB / CHAPTER</th>
            <th style="padding:10px 8px; text-align:center;">TIER & STATUS</th>
            <th style="padding:10px 8px;">TOTAL DONASI</th>
            <th style="padding:10px 8px; text-align:center; width:130px;">AKSI</th>
          </tr>
        </thead>
        <tbody>
          ${filtered.length === 0 ? `
            <tr>
              <td colspan="6" style="text-align:center; padding:32px; color:var(--text-muted);">
                Tidak ada data member yang sesuai dengan filter pencarian.
              </td>
            </tr>
          ` : filtered.map(m => `
            <tr style="border-bottom:1px solid var(--chrome-border);">
              <td style="font-family:monospace; font-weight:800; color:var(--accent-gold); font-size:0.82rem; padding:10px 8px; white-space:nowrap;">
                ${m.member_id || '<span style="color:var(--accent-red);">[PENDING]</span>'}
              </td>
              <td style="padding:10px 8px;">
                <div style="font-weight:700; color:var(--text-main); font-size:0.88rem; line-height:1.2;">${m.name}</div>
                <div style="font-size:0.75rem; color:var(--text-muted); margin-top:2px;">
                  ${m.email} • <span style="color:var(--accent-gold); font-weight:700;">📞 ${m.phone || '-'}</span>
                </div>
              </td>
              <td style="font-size:0.82rem; padding:10px 8px;">${m.club || 'HQ MB INA'}</td>
              <td style="padding:10px 8px; text-align:center;">
                <div style="display:flex; flex-direction:column; gap:4px; align-items:center;">
                  ${getTierBadge(m.tier)}
                  ${getStatusBadge(m.status)}
                </div>
              </td>
              <td style="font-weight:800; color:var(--primary-emerald); font-size:0.85rem; padding:10px 8px; white-space:nowrap;">
                Rp ${new Intl.NumberFormat('id-ID').format(m.total_donation || 0)}
              </td>
              <td style="padding:10px 8px; text-align:center;">
                <div style="display:inline-flex; gap:4px; justify-content:center;">
                  <button class="role-pill-btn" style="border-color:var(--accent-blue); color:var(--accent-blue); width:30px; height:30px; padding:0; display:inline-flex; align-items:center; justify-content:center; font-size:0.85rem; border-radius:6px; cursor:pointer;" onclick="AppEngine.openM3MemberDetail('${m.id}')" title="Detail Profile Member">👁️</button>
                  <button class="role-pill-btn" style="border-color:var(--accent-gold); color:var(--accent-gold); width:30px; height:30px; padding:0; display:inline-flex; align-items:center; justify-content:center; font-size:0.85rem; border-radius:6px; cursor:pointer;" onclick="AppEngine.openEditM3MemberModal('${m.id}')" title="Edit Data Member">✏️</button>
                  <button class="role-pill-btn" style="border-color:var(--primary-emerald); color:var(--primary-emerald); width:30px; height:30px; padding:0; display:inline-flex; align-items:center; justify-content:center; font-size:0.85rem; border-radius:6px; cursor:pointer;" onclick="AppEngine.openM3AddDonationModal('${m.id}')" title="Catat Donasi & Tier">💰</button>
                  <button class="role-pill-btn" style="border-color:var(--accent-red); color:var(--accent-red); width:30px; height:30px; padding:0; display:inline-flex; align-items:center; justify-content:center; font-size:0.85rem; border-radius:6px; cursor:pointer;" onclick="AppEngine.deleteM3Member('${m.id}')" title="Hapus Member">🗑️</button>
                </div>
              </td>
            </tr>
          `).join('')}
        </tbody>
      </table>
      <div style="margin-top:14px; font-size:0.78rem; color:var(--text-muted); display:flex; justify-content:space-between; align-items:center;">
        <span>Menampilkan ${filtered.length} dari ${this.m3Data.members.length} total member MB INA</span>
        <span style="color:var(--accent-gold); font-weight:700;">Database Live: Supabase Cloud PostgreSQL</span>
      </div>
    `;